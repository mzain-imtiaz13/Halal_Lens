<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title><%= subject %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      /* Basic reset */
      body, table, td, a {
        -webkit-text-size-adjust: 100%;
        -ms-text-size-adjust: 100%;
        text-size-adjust: 100%;
      }
      table, td {
        mso-table-lspace: 0pt;
        mso-table-rspace: 0pt;
      }
      img {
        border: 0;
        height: auto;
        line-height: 100%;
        outline: none;
        text-decoration: none;
      }
      table {
        border-collapse: collapse !important;
      }
      body {
        margin: 0 !important;
        padding: 0 !important;
        width: 100% !important;
        background-color: #f3f4f6;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          sans-serif;
      }
      .preheader {
        display: none;
        max-height: 0;
        max-width: 0;
        opacity: 0;
        overflow: hidden;
        mso-hide: all;
        font-size: 1px;
        line-height: 1px;
        color: #111827;
      }
      .wrapper {
        width: 100%;
        table-layout: fixed;
        background-color: #f3f4f6;
        padding: 24px 0;
      }
      .main {
        width: 100%;
        max-width: 600px;
        margin: 0 auto;
        background-color: #ffffff;
        border-radius: 16px;
        border: 1px solid #e5e7eb;
        box-shadow: 0 12px 30px rgba(15, 23, 42, 0.08);
      }
      .inner {
        padding: 24px 24px 28px;
      }
      .brand {
        font-size: 18px;
        font-weight: 700;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: #111827;
      }
      .brand span {
        color: #16a34a;
      }
      .tagline {
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: 0.18em;
        color: #6b7280;
        margin-top: 4px;
      }
      .title {
        margin-top: 24px;
        font-size: 22px;
        font-weight: 700;
        color: #111827;
      }
      .intro {
        margin-top: 12px;
        font-size: 14px;
        color: #374151;
        line-height: 1.5;
      }
      .intro p {
        margin: 4px 0;
      }
      .card {
        margin-top: 16px;
        padding: 14px 16px;
        border-radius: 12px;
        background: radial-gradient(circle at top left, #bbf7d022, #f9fafb);
        border: 1px solid #e5e7eb;
      }
      .card-title {
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 0.18em;
        color: #6b7280;
        margin-bottom: 8px;
      }
      .card-table {
        width: 100%;
        font-size: 13px;
        color: #374151;
      }
      .card-table td {
        padding: 3px 0;
      }
      .card-table td.label {
        color: #6b7280;
        width: 40%;
      }
      .card-table td.value {
        font-weight: 500;
      }
      .cta-wrap {
        margin-top: 20px;
      }
      .cta-button {
        display: inline-block;
        padding: 10px 18px;
        border-radius: 999px;
        background: linear-gradient(135deg, #22c55e, #16a34a);
        color: #ecfdf5 !important;
        text-decoration: none;
        font-size: 13px;
        font-weight: 600;
        letter-spacing: 0.06em;
        text-transform: uppercase;
      }
      .cta-button span {
        padding-left: 4px;
      }
      .note {
        margin-top: 12px;
        font-size: 12px;
        color: #6b7280;
        line-height: 1.5;
      }
      .divider {
        margin-top: 20px;
        border-top: 1px solid #e5e7eb;
      }
      .footer {
        margin-top: 14px;
        font-size: 11px;
        color: #9ca3af;
        line-height: 1.6;
      }
      .footer a {
        color: #6b7280;
        text-decoration: underline;
      }
      .muted {
        color: #9ca3af;
      }
      @media (max-width: 600px) {
        .inner {
          padding: 20px 16px 22px;
        }
        .title {
          font-size: 20px;
        }
      }
    </style>
  </head>
  <body>
    <!-- Preheader text -->
    <div class="preheader"><%= preheader || '' %></div>

    <table border="0" cellpadding="0" cellspacing="0" class="wrapper">
      <tr>
        <td align="center">
          <table border="0" cellpadding="0" cellspacing="0" class="main">
            <tr>
              <td class="inner">
                <!-- Brand -->
                <div class="brand">
                  HALAL<span>LENS</span>
                </div>
                <div class="tagline">Scan • Verify • Feel Confident</div>

                <!-- Title -->
                <div class="title"><%= title %></div>

                <!-- Intro -->
                <div class="intro">
                  <% if (introLines && introLines.length) { %>
                    <% introLines.forEach(function(line){ %>
                      <p><%= line %></p>
                    <% }) %>
                  <% } %>
                </div>

                <!-- Details card -->
                <% if (detailItems && detailItems.length) { %>
                  <div class="card">
                    <div class="card-title"><%= detailTitle || 'Details' %></div>
                    <table class="card-table" role="presentation">
                      <tbody>
                        <% detailItems.forEach(function(item){ %>
                          <tr>
                            <td class="label"><%= item.label %></td>
                            <td class="value"><%= item.value %></td>
                          </tr>
                        <% }) %>
                      </tbody>
                    </table>
                  </div>
                <% } %>

                <!-- CTA -->
                <% if (ctaUrl && ctaLabel) { %>
                  <div class="cta-wrap">
                    <a href="<%= ctaUrl %>" target="_blank" class="cta-button">
                      <%= ctaLabel %><span>→</span>
                    </a>
                  </div>
                <% } %>

                <!-- Note -->
                <% if (footerNote) { %>
                  <div class="note"><%= footerNote %></div>
                <% } %>

                <div class="divider"></div>

                <!-- Footer -->
                <div class="footer">
                  <p>
                    If you didn't request or expect this email, you can safely ignore it.
                  </p>
                  <p class="muted">
                    Halal Lens · Helping you make confident halal choices every day.
                  </p>
                  <% if (supportUrl) { %>
                    <p>
                      Need help? Email us at
                      <a href="mailto:<%= supportEmail %>"><%= supportEmail %></a>
                      or visit
                      <a href="<%= supportUrl %>" target="_blank"
                        >our support page</a
                      >.
                    </p>
                  <% } %>
                </div>
              </td>
            </tr>
          </table>

          <!-- Spacer bottom -->
          <table width="100%" border="0" cellspacing="0" cellpadding="0">
            <tr><td height="24">&nbsp;</td></tr>
          </table>
        </td>
      </tr>
    </table>
  </body>
</html>
